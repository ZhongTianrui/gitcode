#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    int n; cin >> n;
    if (n == 15 or n == 20 or n == 21) cout << "NO";
    else cout << "YES";
    return 0;
}