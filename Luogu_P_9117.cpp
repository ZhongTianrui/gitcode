#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    int T; cin >> T;
    while (T --) {
        int n, m, q;
        int Hang[100005], Lie[100005];
        cin >> n >> m >> q;
        while (q --) {
            
        }
    }
    return 0;
}